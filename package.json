{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "URL shortener — single-origin deploy (backend serves the built frontend).",
  "private": true,
  "packageManager": "pnpm@10.29.1",
  "scripts": {
    "build": "pnpm -C frontend install --frozen-lockfile && pnpm -C frontend build && pnpm -C backend install --frozen-lockfile && pnpm -C backend build",
    "start": "pnpm -C backend start"
  }
}
